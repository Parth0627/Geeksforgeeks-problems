#include <stdio.h>

int main()
{
    printf("%lu is the size of an integer in your system\n",sizeof(int));
    
    printf("%lu is the size of an float in your system\n",sizeof(float));
    
    printf("%lu is the size of an char in your system\n",sizeof(char));
    
    printf("%lu is the size of an double in your system\n",sizeof(double));
    
    return 0;
}